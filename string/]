#include<stdio.h>
int mystrlen(char* s)
{
	int count=0;
	int i=0;
	while(*s=='\0')
	{
		count++;
	}
	return count;
}
int main()
{
	int len;
	char s[20]="hello";
	len=mystrlen(s);
	printf("count of string is: ",len);
}
